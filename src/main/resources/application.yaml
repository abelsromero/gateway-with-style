management:
  endpoint:
    gateway:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
spring:
  cloud:
    gateway:
      routes:
        - id: test
          uri: "https://httpbin.org"
          predicates:
            - Path=/test/**
          filters:
            - StripPrefix=1
